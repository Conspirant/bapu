Drop your recording here as `gandhi-message.mp3` (or change `audioConfig.src`
in `src/data/site-content.ts`). Until a file exists, the player runs in a
silent demo mode. Use only audio you have the rights to publish.
